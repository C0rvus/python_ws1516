mainController.commonCrimeView = (function () {  var that = {},

      init = function () {
          console.log("commonCrimeView is up");
          return that;
      },

      initModules = function () {
      }

    that.init = init;
    return that;
})();
