mainController.commonCrimeController = (function () {
    var that = {}

        init = function () {
            console.log("commonCrimeController is up");
            return that;
        }

    that.init = init;
    return that;
})();
